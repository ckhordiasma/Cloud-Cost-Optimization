[project]
name = "cloud-cost-optimization"
version = "0.1.0"
description = "AWS cloud resource cleanup and cost optimization tools"
requires-python = ">=3.10"
dependencies = [
    "boto3~=1.26.111",
    "botocore>=1.29.111,<1.30.0",
    "requests~=2.28.2",
    "smartsheet-python-sdk~=3.0.2",
]
